{
  "name": "FX-EVM-Blocks",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy FX-EVM-Blocklytics --ipfs https://testnet-fx-ipfs.functionx.io --node https://testnet-graph-node.functionx.io/admin",
    "create": "graph create FX-EVM-Blocklytics --node https://testnet-graph-node.functionx.io/admin",
    "create-local": "graph create --node http://localhost:8020/ FX-EVM-Blocklytics",
    "remove-local": "graph remove --node http://localhost:8020/ FX-EVM-Blocklytics",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 FX-EVM-Blocklytics"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.17.1",
    "@graphprotocol/graph-ts": "0.17.0"
  }
}
